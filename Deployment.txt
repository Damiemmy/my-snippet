1.) install gunicorn

2.) "FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements.txt .
RUN pip install --upgrade pip && pip install -r requirements.txt

COPY . .

RUN python manage.py collectstatic --no-input

EXPOSE 8000

CMD ["gunicorn", "ecommerce_site.wsgi:application", "--bind", "0.0.0.0:8000"]"
3.)
    STATIC_ROOT = BASE_DIR / "staticfiles"

4.)DEBUG = False
5.) environment variables:
    DJANGO_SECRET_KEY=your-secret-key
    DEBUG=False
    DATABASE_URL=postgres://...
6.) install drf-yasg and include it in settings.installed_apps configure in urls.py adding "title" and "description" (note: django and djangorestframework must be installed for yasg to work)
7.) add all packages to requirements.txt (very important):

❌ What You Should NOT Do
Mistake	Why
Using runserver	Not production
Using docker-compose	Render ignores it
Using volumes	Render containers are read-only
Exposing secrets in Dockerfile	Security risk

ERROR IN DEPLOYEMENT:
1.) ERROR:-whitenoise.storage.MissingFileError:
The file
'drf-yasg/swagger-ui-dist/swagger-ui-es-bundle-core.js.map'
could not be found

    SOLUTION: replace storage with this "STORAGES = {
    "default": {
        "BACKEND": "django.core.files.storage.FileSystemStorage",
    },
    "staticfiles": {
        "BACKEND": "whitenoise.storage.CompressedStaticFilesStorage",
    },
}

WHITENOISE_MANIFEST_STRICT = False"

FOR FALL BACK DEVELOPMENT SECRET_KEY CONFIGURAION:

    SECRET_KEY = os.environ.get(
        "SECRET_KEY",
        "unsafe-secret-key-for-build-only"
    )


DEPLOYEMENT FOR NEXT.JS APP:
    package.json file must have :
    {
        "scripts": {
            "dev": "next dev",
            "build": "next build",
            "start": "next start -p $PORT"
        }
     }

    {
        "engines": {
            "node": ">=18"
        }
    }

    BUILD COMMAND FOR NEXT.JS ON RENDER: npm install; npm run build
    START COMMAND FOR NEXT.JS ON RENDER: npm install; npm run start