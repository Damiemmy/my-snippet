show create table name;
set SQL_SAFE_UPDATES=0;

MODIFYING TABLES WITH A PRIMARY KEY:
//
ALTER TABLE bio DROP PRIMARY KEY;

    ALTER TABLE bio 
    MODIFY user_id INT AUTO_INCREMENT,
    ADD PRIMARY KEY (user_id);
//

DATES:
//
insert into mydate(newdate,newtime,newdatetime,newtimestamp)
values(current_date(),current_time(),now(),current_timestamp())
//

JOIN TABLES
//
CREATE TABLE Students(
student_id INT AUTO_INCREMENT PRIMARY KEY,
student_name VARCHAR(100)
);

CREATE TABLE Student_info(
id INT AUTO_INCREMENT PRIMARY KEY,
student_id INT,
age INT,
course VARCHAR(100) NOT NULL,

FOREIGN KEY(student_id) REFERENCES Students(student_id)
);

//

//

SELECT students.student_name,student_info.age,student_info.course,students.student_id FROM students
INNER JOIN student_info
ON student_info.student_id=students.

🔥 How Django Performs JOIN
Query:
students = Student.objects.select_related('studentinfo')

👉 Django automatically does JOIN behind the scenes


//
 Note: any record can be selected by specifying the tablename.column to be displayed, the vital aspect is to specify the key word INNER JOIN <selected table to pair name>
 and finally specifying the the foreign key fields that are connected using ON key word

//wild cards(_%):
select * from students
where student_name like'_a%';
where student_name like'd%';
where student_name like'%i';
where student_name like'_a%'
where student_name like'__m%'

//ORDER By:
SELECT * FROM student_info
ORDER BY id desc;
ORDER BY id asc;

//LIMIT:
SELECT * FROM student_info
LIMIT 2 or 0,2
LIMIT 1,2
LIMIT 0,3 or 3

//VIEW:
CREATE VIEW student_name as
SELECT STUDENT_NAME FROM students

//SELECTING A VIEW:
SELECT student_name from student_name
ORDER BY student_name asc



//SUBQUERIES
select *,(select avg(salary )from employees)as salary_allowance from employees
select *,(salary+1 ) as salary_allowance from employees
select *,(salary) as salary_allowance from employees
select avg(salary)  from employees

//GROUP_BY:Avoid repetition of queries
select First_Name from employees
group by First_Name

//DROPPIN A PRIMARY KEY:
ALTER TABLE STUDENT_INFO DROP PRIMARY KEY

//DROPPIN A FOREIGN KEY:
ADD CONSTRAINT fk_student_id 
FOREIGN KEY(STUDENT_ID) REFERENCES(STUDENT_ID)