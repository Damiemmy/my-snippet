NumPy:numpy performs numerical computation ,it stands for Numerical Python which gives us a powerful numerical data structure called an Array: or
Numpy It's a Python library designed primarily for efficient numerical computation and multidimensional arrays.

In addition operation:With ordinary Python:

70 → 80
80 → 90
90 → 100

You'd normally need a loop or comprehension.

With numerical computing libraries, we can express operations much more naturally over the whole collection.

This idea is called vectorization.

And vectorization is one of the concepts I want you to understand deeply.


2.)
🧠 6. Features Become Numbers

Remember our earlier discussion?

Suppose we want to predict a student's performance.

Our dataset could become:

age | study_hours | attendance
20  |      5      |     90
19  |      7      |     95
21  |      3      |     70
20  |      8      |     98

Numerically:

[
 [20, 5, 90],
 [19, 7, 95],
 [21, 3, 70],
 [20, 8, 98]
]

That's a matrix.

And now you're beginning to see the bridge:

Real-world data
       ↓
Structured dataset
       ↓
Numerical representation
       ↓
NumPy arrays
       ↓
Machine-learning algorithms