📚 What I Learned Today
One-to-many relationships put the foreign key on the many side.
Django ForeignKey represents these relationships.
related_name gives us convenient reverse relationships.
UniqueConstraint protects database integrity.
CASCADE controls what happens to dependent records.
Domain entities can live together when they form one cohesive bounded domain.

📚 What I Learned Today
Selectors own reusable database read operations.
Serializers convert model instances into API representations.
Views coordinate HTTP requests and responses.
A read-only ListAPIView is appropriate for our first GET endpoint.
Not every database query requires a service.
API capabilities should be deliberately exposed.

📚 What I Learned Today
select_related() helps prevent unnecessary database queries for foreign-key relationships.
We can expose related information without duplicating database fields.
Established architectural patterns should become reusable mental templates.
We don't need the same depth of explanation for every repetitive endpoint.
