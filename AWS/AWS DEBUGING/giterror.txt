#1.) fail to push code to github due to exposed secret 

SOLUTION:fix secret with .env and run this code on terminal before push:
git filter-branch --force --index-filter \
"git rm --cached --ignore-unmatch Backend/settings.py" \
--prune-empty --tag-name-filter cat -- --all