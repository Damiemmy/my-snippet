1.)Database refuse to drop:
    solution:
       - postgres=# SELECT pg_terminate_backend(pid)
        FROM pg_stat_activity
        WHERE datname = 'airbnb_db';
        pg_terminate_backend 

        #then run:
        -DROP DATABASE airbnb_db;

        #or try:
        DROP DATABASE airbnb_db WITH (FORCE);